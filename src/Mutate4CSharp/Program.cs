namespace Microsoft.Mutate4CSharp;

/// <summary>
/// Entry point for the mutate4csharp CLI. This is a scaffolding stub; real command-line
/// wiring is introduced in later tasks.
/// </summary>
public static class Program
{
    /// <summary>
    /// Runs the CLI.
    /// </summary>
    /// <returns>The process exit code.</returns>
    public static int Main()
    {
        return 0;
    }
}
